#include<Stdio.h>
int main(void)
{
  int num1 = 3; //정수형 변수 num1에 정수 3을 넣음;
  double num2 = 3.14; //실수형 변수 num2에 실수 3.14를 넣음
  char str = 'a'; //문자형 변수 str에 문자 a를 넣음 (char str = '3'과 int num1 = 3은 다름)
  
  printf("%d\n", num1); //%d는 정수형 변수를 부르겠다는 뜻 , 큰 따옴표 밖에 부를 정수형 변수를 적으면 출력이 됌(\n은 줄을 바꿔서 출력 하겠다는 뜻)
  printf("%lf\n", num2); //%lf는 실수형 변수를 부르겠다는 뜻 , 큰 따옴표 밖에 부를 실수형 변수를 적으면 출력이 됌
  printf("%c", str); //%c는 문자형 변수를 부르겠다는 뜻 , 큰 따옴표 밖에 부를 문자형 변수를 적으면 출력이 됌(%s도 존재 한다 %s는 문자열을 출력하는것을 의미 한다) 이건 문자열이 아니기 때문에 %c만 쓴것이다.
}
