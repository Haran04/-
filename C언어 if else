#include<stdio.h>
int main(void)
{
  int a;
  scanf("%d", &a);
  
  if(a>0) //입력 받은 a가 0보다 크다면 크다를 출력 (if문에는 ;을 쓰지 않는다)
  {
    printf("크다");
  }
  else //입력 받은 a가 0이거나 0보다 작다면 (else에는 조건식을 쓰지 않는다)
  {
    printf("작다"); //작다를 출력
  }
}
